import { Box, styled } from "@mui/material";

export const StyledBox = styled(Box)(() => ({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    padding: '2rem',
    backgroundColor: '#333',
    color: 'white',
    width: '100%',
    bottom: 0,
    left: 0,
}));
  
